name: Config Test - Execute Jupyter Notebooks
on:
  workflow_dispatch:
    inputs:  
      python-version:
        required: true
        type: string

  workflow_call:
    inputs:  
      python-version:
        required: true
        type: string
    secrets:
      CASJOBS_USERID:
        description: 'CASJOBS user ID'
        required: false
      CASJOBS_PW:
        description: 'CASJOBS password'
        required: false

env:
  CASJOBS_PW: ${{ secrets.CASJOBS_PW }}
  CASJOBS_USERID: ${{ secrets.CASJOBS_USERID }}

jobs:
  generate-matrix:
    name: Generate Notebook Matrix
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.build_matrix.outputs.matrix }}
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3
      - name: Check current directory
        run: |
          echo "Current directory: $(pwd)"
          echo "Listing files in current directory:"
          ls -la
          
      - name: Check if notebooks directory exists
        run: |
          if [ ! -d "notebooks" ]; then
            echo "Error: 'notebooks' directory not found!";
            exit 1;
          fi

      - name: Find notebooks and build matrix
        id: build_matrix
        shell: bash
        run: |
          # List all Jupyter notebooks under 'notebooks/' (null-separated to handle spaces)
          mapfile -d $'\0' -t notebooks < <(find notebooks -type f -name '*.ipynb' -print0)
          default_runner="ubuntu-latest"
          # Start building the JSON string for the matrix
          matrix_json='{"include":['
          separator=""
          for file in "${notebooks[@]}"; do
            # Skip if for some reason the entry is empty
            [[ -z "$file" ]] && continue
            runner="$default_runner"
            # Check if the notebook path is in the config file (match exact path followed by a colon)
            if grep -F -m1 -q "${file}:" ci_config.txt; then
              # Get the first matching line and extract text after the first colon
              config_line=$(grep -F -m1 "${file}:" ci_config.txt)
              runner_config=${config_line#*:}        # remove the path and colon
              runner_config=$(echo "$runner_config" | xargs)  # trim whitespace
              if [[ -n "$runner_config" ]]; then
                runner="$runner_config"
              fi
            fi
            # Escape quotes and backslashes in file and runner names for JSON safety
            file_json=$(printf '%s' "$file"   | sed 's/\\/\\\\/g; s/\"/\\"/g')
            runner_json=$(printf '%s' "$runner" | sed 's/\\/\\\\/g; s/\"/\\"/g')
            # Append this notebook's entry to the JSON array
            matrix_json+="${separator}{\"file\":\"${file_json}\",\"runner\":\"${runner_json}\"}"
            separator=","
          done
          matrix_json+=']}'
          # Output the matrix JSON (as a single line) to be used by subsequent jobs
          echo "matrix=${matrix_json}" >> $GITHUB_OUTPUT


     # Execute-Notebooks:       
      - name: Install dependencies
        run: |
          echo "DEBUG ---"
          pwd
          echo "Path to req's: $(dirname ${{ matrix.file }})/requirements.txt"
          ls $(dirname ${{ matrix.notebook }})
          echo "---"
          
          if [ -f $(dirname "${{ matrix.file }}")/pre-requirements.sh ]; then
            chmod +x $(dirname "${{ matrix.file }}")/pre-requirements.sh
            ./$(dirname "${{ matrix.file }}")/pre-requirements.sh
          fi
          if [ -f $(dirname "${{ matrix.file }}")/pre-requirements.txt ]; then
            pip install -r $(dirname "${{ matrix.file }}")/pre-requirements.txt
          fi
          if [ -f pre-requirements.txt ]; then
            pip install -r pre-requirements.txt
            echo "Found Pre-Reqs"
          fi
          pip install -r $(dirname ${{ matrix.file }})/requirements.txt
          #pip install pytest
          #pip install nbval
          pip install nbconvert
          #pip install bandit
          pip install ipykernel
          
      - name: Execute Jupyter Notebook
        run: |
          export CASJOBS_PW="$CI_CASJOBS_PW"
          export CASJOBS_USERID="$CI_CASJOBS_USERID"
          echo "Executing ${{ matrix.file }} on runner: ${{ toJson(matrix.runner) }}"
          # Find the full path of the notebook and execute it
          notebook_path=$(find notebooks -name "${{ matrix.file }}")
          jupyter nbconvert --to notebook --execute "$notebook_path" --output "${{ matrix.file }}"


#      - name: Validate notebooks
#        run: |
#          pytest --nbval "${{ matrix.notebook }}"

#      - name: Scan with Bandit
#        run: |
#          pip install ipython
#          jupyter nbconvert --to script ${{ matrix.notebook }}
#          pyfile=$(echo "${{ matrix.notebook }}" | sed 's/\.ipynb$/.py/')
#          bandit -lll -iii $pyfile
